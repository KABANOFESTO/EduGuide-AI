"use client";

import LiveProfilePage from "@/components/account/LiveProfilePage";

export default function EmailStaffProfilePage() {
    return (
        <LiveProfilePage
            heading="Staff Profile"
            description="Manage the email staff account connected to the live backend."
        />
    );
}
