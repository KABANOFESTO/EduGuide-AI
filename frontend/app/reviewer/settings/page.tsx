"use client";

import AccountSettingsPage from "@/components/account/AccountSettingsPage";

export default function ReviewerSettingsPage() {
    return (
        <AccountSettingsPage
            heading="Reviewer Settings"
            description="Configure reviewer preferences and password from the live backend."
        />
    );
}
